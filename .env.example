# Optional: enables the enhanced Gemini-powered planner in the generated test plan.
GEMINI_API_KEY=

# Optional: overrides the default Gemini model.
# If omitted, SpecPilot falls back to gemini-2.5-flash.
GEMINI_MODEL=gemini-2.5-flash
