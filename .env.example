# Optional: enables the enhanced AI risk memo in the generated test plan.
OPENAI_API_KEY=

# Optional: overrides the default model used for the AI memo.
# If omitted, SpecPilot falls back to gpt-4.1-mini.
OPENAI_MODEL=gpt-4.1-mini
